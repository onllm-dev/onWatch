# ============================================================
# SynTrack - Synthetic API Usage Tracker
# Copy this file to .env and fill in your values
# ============================================================

# --- Synthetic API ---
# Your Synthetic API key (required)
# Get it from: https://synthetic.new/settings/api
SYNTHETIC_API_KEY=syn_your_api_key_here

# --- Polling Configuration ---
# Interval in seconds between API polls (default: 60)
# Min: 10, Max: 3600
SYNTRACK_POLL_INTERVAL=60

# --- Web Dashboard ---
# Port for the web dashboard (default: 8932)
SYNTRACK_PORT=8932

# --- Admin Authentication ---
# Username and password for dashboard access
SYNTRACK_ADMIN_USER=admin
SYNTRACK_ADMIN_PASS=changeme

# --- Database ---
# Path to SQLite database file (default: ./syntrack.db)
SYNTRACK_DB_PATH=./syntrack.db

# --- Logging ---
# Log level: debug, info, warn, error (default: info)
# In background mode (default), logs go to .syntrack.log
# In debug mode (--debug), logs go to stdout
SYNTRACK_LOG_LEVEL=info

# --- Z.ai / ZHIPU API (optional, for future integration) ---
# Your Z.ai API key for Claude Code usage monitoring
# Get it from: https://z.ai dashboard
# Supports two base URLs:
#   https://api.z.ai/api         (primary)
#   https://open.bigmodel.cn/api (ZHIPU mirror, identical data)
ZAI_API_KEY=your_zai_api_key_here
